Red Black Tree

An implementation of a Red-Black Tree in C.

Build & Compile

To compile the project, run:

make buid

To execute the program, use:

./main <file-path>

Replace <file-path> with the path to the input file to be processed by the tree.

Example:

make build
./main ./test/2.txt